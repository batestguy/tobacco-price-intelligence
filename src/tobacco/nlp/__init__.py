"""Sentiment scoring (INTRO.txt §3).

``finbert`` needs torch and so runs only in ``score.yml``. ``vader`` is a lexicon
with no model weights and runs anywhere, including inline with the scrapers.
"""
