# Dependencies for the GitHub Actions compute jobs.
#
# NOTE: torch is deliberately absent. It is installed only by score.yml, from the
# CPU wheel index (--index-url https://download.pytorch.org/whl/cpu). The default
# PyPI wheel pulls ~2 GB of CUDA that a GitHub runner cannot use.

# core
pandas==2.2.3
pyarrow==18.1.0
requests==2.32.3
python-dateutil==2.9.0.post0

# scraping
beautifulsoup4==4.12.3
lxml==5.3.0
lxml_html_clean==0.4.1
newspaper3k==0.2.8
feedparser==6.0.11

# nlp
nltk==3.9.1
transformers==4.46.3
huggingface-hub==0.26.5

# modelling
xgboost==2.1.3
scikit-learn==1.5.2
scipy==1.14.1
joblib==1.4.2

# llm memo
groq==0.13.1
